<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:maxHeight="350dp"
    android:orientation="vertical">

    <!-- Заголовок с шестерёнкой -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:title="Перевод"
        app:titleTextColor="@android:color/white"
        app:navigationIcon="?attr/homeAsUpIndicator"
        app:navigationContentDescription="Закрыть">

        <!-- Кнопка "Сохранить в банк слов" -->
        <ImageView
            android:id="@+id/saveToVocabularyButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:padding="12dp"
            android:src="@drawable/ic_baseline_bookmark_add_24"
            app:tint="@android:color/white"
            android:contentDescription="Сохранить в банк слов"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/settingsButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="end"
            android:padding="12dp"
            android:src="@drawable/ic_baseline_settings_24"
            app:tint="@android:color/white"
            android:contentDescription="Настройки языка" />

    </androidx.appcompat.widget.Toolbar>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Исходный текст:"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <!-- Исходный текст с фиксированной высотой -->
        <TextView
            android:id="@+id/originalText"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:padding="12dp"
            android:background="#f0f0f0"
            android:textSize="14sp"
            android:textColor="@android:color/black"
            android:scrollbars="vertical"
            android:maxLines="100"
            android:layout_marginBottom="16dp" />

        <!-- Информация о выбранных языках -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="16dp">

            <TextView
                android:id="@+id/sourceLanguageText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Английский →"
                android:textSize="14sp"
                android:textStyle="bold"
                android:gravity="center" />

            <TextView
                android:id="@+id/targetLanguageText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Русский"
                android:textSize="14sp"
                android:textStyle="bold"
                android:gravity="center" />

        </LinearLayout>

        <!-- Кнопка скачивания модели -->
        <Button
            android:id="@+id/downloadButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Скачать модель для перевода"
            android:visibility="gone"
            android:layout_marginBottom="16dp" />

        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:layout_marginBottom="8dp" />

        <TextView
            android:id="@+id/loadingText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:gravity="center"
            android:visibility="gone"
            android:layout_marginBottom="16dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Перевод:"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <!-- Перевод с фиксированной высотой -->
        <TextView
            android:id="@+id/resultText"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:padding="12dp"
            android:background="#f0f0f0"
            android:textSize="14sp"
            android:textColor="@android:color/black"
            android:scrollbars="vertical"
            android:maxLines="100"
            android:visibility="gone" />

    </LinearLayout>

</LinearLayout>